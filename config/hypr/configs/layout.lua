hl.config({
    scrolling = {
        fullscreen_on_one_column = true,
        column_width = 0.5,
        focus_fit_method = 1,
    },

    dwindle = {
        -- pseudotile = true,
        preserve_split = true,
        force_split = 2,
    },

    master = {
        new_status = "slave",
    },
})
